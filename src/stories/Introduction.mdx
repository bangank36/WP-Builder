import { Meta } from '@storybook/addon-docs'

<Meta title="Introduction" />

# JsonForms Examples

This is a collection of stories in different renderers for [Eclipesource JsonForms](https://github.com/eclipsesource/jsonforms).

All [examples](https://github.com/eclipsesource/jsonforms/tree/master/packages/examples) from the repo will be exported and rendered using following renderers

<table>
    <tr>
        <th>Renderers set</th>
        <th>Status</th>
        <th>Base</th>
    </tr>
    <tr>
        <td>[React Material](https://github.com/eclipsesource/jsonforms/tree/master/packages/material-renderers)</td>
        <td>Official</td>
        <td>React</td>
    </tr>
    <tr>
        <td>[React Vanilla](https://github.com/eclipsesource/jsonforms/tree/master/packages/vanilla-renderers)</td>
        <td>Official</td>
        <td>React</td>
    </tr>
    <tr>
        <td>[Gutenberg Renderer](https://github.com/bangank36/WP-Builder)</td>
        <td>Experimental</td>
        <td>React</td>
    </tr>
</table>

## 🚀 Getting Started

Install the library and peer-dependencies in your application

Pass the JSON Schema to the `JsonForms` component.

```jsx
import { JsonForms } from "@jsonforms/react";
import { vanillaCells, vanillaRenderers } from '@jsonforms/vanilla-renderers';
import { enumExample } from '@jsonforms/examples'; 
import React from 'react';

export const MyForm = () => {
  const [data, setData] = useState(initialData);

  return <JsonForms
          schema={exampleData.schema}
          uischema={exampleData.uischema}
          data={exampleData.data}
          renderers={vanillaRenderers}
          cells={vanillaCells}
          onChange={({ data, _errors }) => {
            setData(data);
          }}
        />
}
```

For further examples see the stories.

## 🔗 Links

- [Eclipesource JsonForms](https://github.com/eclipsesource/jsonforms).
- [JsonForms official renderers set](https://jsonforms.io/docs/renderer-sets/).
- [Wordpress Components package](https://github.com/WordPress/gutenberg/tree/trunk/packages/components) is based of the Gutenberg renderers, this experimental renderers introduces a new Navigator Layout to display nested data structure into navigatable layout.
- [GutenBuilder blog](https://gutenbuilder.wordpress.com/): dedicated place for sharing development notes for Gutenberg renderers.